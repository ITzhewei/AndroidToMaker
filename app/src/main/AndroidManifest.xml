<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.john.androidtomaker"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.ResultActivity">
        </activity>

        <service android:name=".service.WeatherService"/>
        <!-- <receiver android:name=".broadcastreceiver.HelloBroaderCast"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="hello"/> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <activity android:name=".Model.dagger.DaggerActivity">
        </activity>
        <activity android:name=".Model.RxBus.RxActivity">
        </activity>
    </application>

</manifest>